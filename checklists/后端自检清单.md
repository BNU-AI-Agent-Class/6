# 后端自检清单

> 门禁：全部通过才能进入 SHIP 上线阶段。
>
> B4 最近验证：2026-07-12，状态：**通过**。

## B4 验证证据

- `python -m unittest -q test_safety.py`：31 项测试全部通过
- `GET /api/health`：200，版本 `3.3.0`，模型 `deepseek/deepseek-v4-flash`
- 真实普通对话：`is_crisis=false`，不含热线或紧急服务话术
- 真实危机对话：`is_crisis=true`，返回全国统一心理援助热线 12356
- CORS 预检：跨端口 5500 → 8000 返回 200
- 前端浏览器联调：桌面、390px 移动端、断线降级均通过

## 关 1：Key 安全

- [x] API key 由 `os.getenv("OPENROUTER_API_KEY")` 或 `os.getenv("OPENAI_API_KEY")` 读取
- [x] 代码中无硬编码真实 key（`grep -rn sk- backend/main.py` 为零）
- [x] `.env.example` 仅含占位符，`.env` 在 `.gitignore` 中

## 关 2：危机拦截

- [x] `evaluate_safety()` 只根据用户消息做确定性绿/黄/橙/红判定
- [x] 黄灯（如“活着没意思”“我撑不下去了”）确认安全但不标红；明确当前伤害意图才进入红灯
- [x] c3 子 agent `safety_review()` 会拒绝普通用户消息上的过度危机转介
- [x] 绿灯出口确定性拦截“你安全吗/无法保密/24 小时热线/紧急服务”等误生成话术
- [x] 旧危机 AI 回复不进入模型历史，客户端 `system` role 返回 422
- [x] 31 项边界测试覆盖三份导出记录的全部用户消息、旧历史污染、课程讨论、否认、第三方、黄灯、明确意图和迫近危险

## 关 3：优雅降级

- [x] LLM API 调不通时返回兜底话术（`stage: "fallback"`），不抛 500
- [x] `/api/health` 可被前端或监控系统轮询
- [x] 模型请求使用 `trust_env=False`，不继承本机失效的 `HTTP(S)_PROXY`
- [x] OpenRouter 默认模型为已在当前地区实测返回 200 的 `deepseek/deepseek-v4-flash`
- [x] 上游异常写入 PowerShell 日志，不再静默吞掉真实错误

## 关 4：依赖与运行

- [x] `requirements.txt` 包含五个依赖：fastapi / uvicorn / httpx / pydantic / python-dotenv
- [x] `uvicorn main:app --port 8000` 可正常启动
- [x] `/api/chat` 正常返回 `{reply, is_crisis, model, stage}`

## 关 5：工作流能力

- [x] c0 JSON 协议 — LLM 消息循环
- [x] c1 工具箱 — 6 个 skills 注入 system prompt（危机转介置顶）
- [x] c2 TodoWrite — `[阶段: xxx]` 标记 + `stage` 字段
- [x] c3 子 agent — `safety_review()` 安全复核
- [x] c4 压缩 — `compact_messages()` 15 条阈值
- [x] c5 权限门 — 无 bash/rm/os.system，仅文本对话

## 关 6：Agent 配置

- [x] `agent.md` 三段式人设：身份 / 语气 / 硬边界
- [x] 6 个 skill 文件完整，危机转介置顶拼接
- [x] system prompt 含安全提醒和 c2 阶段指引

## 关 7：API 契约

- [x] `POST /api/chat` 入参 `{messages: [{role, content}]}`，role 仅允许 `user/assistant`
- [x] 出参 `{reply, is_crisis, model, stage}`
- [x] `GET /api/health` 返回 `{status: "ok", model}`
- [x] CORS 中间件 `allow_origins=["*"]`（上线后收紧）

---

*MIT License｜郑先隽，北京师范大学心理学部教授，人本AI设计与创新*
