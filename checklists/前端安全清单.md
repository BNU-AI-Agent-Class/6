# 前端安全清单

> 门禁：全部通过才能进入 SHIP 上线阶段。
>
> B4 最近验证：2026-07-12，状态：**通过**。官方 BUILD 清单与本项目扩展项均已实测。

## B4 验证证据

- 官方密钥扫描规则 `sk-|openrouter.*key|authorization.*bearer`：前端目录零匹配
- 前端调用审计：仅请求 `BACKEND + /api/health` 与 `BACKEND + /api/chat`；HTTP 页面自动沿用当前主机名，手机访问时不会错误指向手机自身的 `localhost`
- Git 敏感文件审计：未追踪 `.env` 或密钥文件，`backend/.env` 命中 `.gitignore`
- `node test_frontend_contract.js`：`frontend_contract=ok`
- `node test_b4_e2e.js`：桌面普通/危机对话、断线降级、390px 移动端全部通过；局域网入口 `172.23.169.227:5500` 与后端 `:8000` 均返回 200
- 真实接口：普通“你好，你能帮我梳理一下情绪吗”不触发危机或热线；明确当前自伤意图触发危机并返回 12356
- CORS：`OPTIONS /api/chat` 从 `http://127.0.0.1:5500` 返回 200，含 `access-control-allow-origin`
- 本地入口：前端 `http://127.0.0.1:5500`，后端健康检查 `http://127.0.0.1:8000/api/health`

## 第 1 关：Key 安全

- [x] `grep -rniE "sk-[a-z0-9]{16,}" frontend/` 结果为零
- [x] 前端无任何 `fetch("https://openrouter.ai/...")` 直连模型 API
- [x] 前端只调自己的后端 `BACKEND + /api/chat`

## 第 2 关：危机求助

- [x] 危机求助按钮常驻可见（每页都能看到）
- [x] 点击后发送求助消息，触发后端 `is_crisis` 阻塞 → 返回热线转介
- [x] 中国境内转介使用全国统一心理援助热线 12356

## 第 3 关：优雅降级

- [x] 后端不可达时 `catch(err)` 返回兜底话术
- [x] 普通断线显示中性兜底，不标红、不主动给热线
- [x] 仅明确本人危机意图或紧急帮助按钮在断线时显示真人求助资源
- [x] 页面不白屏、不崩溃、不弹错误堆栈
- [x] 网络恢复后正常通信

## 第 4 关：跨域

- [x] 支持本地文件或静态 HTTP 页面访问，后端 CORS 可接收请求
- [x] CORS 预检通过，响应头含 `access-control-allow-origin`
- [x] 前后端跨端口通信正常（5500 → 8000）

## 第 5 关：移动端

- [x] 手机端 390px 视口无横向溢出，侧边栏收起宽度为 0
- [x] 手机端输入、发送、接收回复正常
- [x] 侧边栏在手机上为滑出式，折叠可回

## 第 6 关：历史污染防护

- [x] 使用 `bnu-agent-ui-v5`，不继续加载旧误判或连接失败会话
- [x] 请求后端时只发送 `user/assistant` 消息
- [x] 过滤前端系统提示和带危机标记的旧 AI 回复
- [x] 真正红灯只展示一次后端危机回复，不追加重复热线卡
- [x] 发送前要求后端版本为 `3.3.0`，拒绝未重启的旧进程

---

*MIT License｜郑先隽，北京师范大学心理学部教授，人本AI设计与创新*
